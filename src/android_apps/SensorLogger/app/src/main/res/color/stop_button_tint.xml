<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- State when enabled -->
    <item android:state_enabled="true" android:color="@color/button_purple"/> <!-- Or your desired enabled outline color if using OutlinedButton effectively -->
    <!-- State when disabled -->
    <item android:state_enabled="false" android:color="@color/disabled_button_gray"/>
    <!-- Default state (optional) -->
    <item android:color="@color/disabled_button_gray"/>
</selector>